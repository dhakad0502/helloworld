pipeline {
    agent any

    stages {
        stage('Source code Checkout from github') {
            steps {
                git 'https://github.com/javahometech/myweb.git'
            }
        }
    }
}
